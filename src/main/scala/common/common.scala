package common

import scala.collection.mutable.ArrayBuffer 
import scala.concurrent.duration._
case class spawn_remote(no_of_zeros: Integer) 
case class remote_work_done(remotely_mined_bitcoins:ArrayBuffer[String])
